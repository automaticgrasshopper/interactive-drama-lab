# 工作进度·2026-07-17

- 完成生产后台模型下拉/手填配置，校验模型留空时跟随生成模型。
- 修复后端保存与页面 JSON 返回问题，统一由生产后台管理 Git 提交和推送。
- 大纲台改为四档输入意图自动识别，去掉手动导入模式；支持文稿和参考图，暂不支持视频。
- 参考图可传入分镜台，用于人物、场景和画风锤定。
- 大纲与分镜页面的规则/执行卡入口已集中到生产后台。

下次可从“用一份实际剧本＋参考图跑完大纲→分镜全链路回归”开始。
